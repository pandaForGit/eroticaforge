# API 根路径（与 Spring Boot server.servlet.context-path + /api 一致；端口以 application.yml 为准，默认 8090）
# 开发态推荐：使用相对路径 + Vite 代理（见 vite.config.ts），避免 CORS
VITE_API_BASE=/api

# 若不用代理，可直连后端（需后端允许跨域或同源部署）：
# VITE_API_BASE=http://localhost:8090/api
